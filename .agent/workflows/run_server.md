---
description: Run the Spring Boot backend server
---

To run the ZenithMind Spring Boot application:

1. Open a terminal in the project root.
2. Execute the following command:

```bash
mvn spring-boot:run
```

Once the server starts, you can access it at:
http://localhost:8081
